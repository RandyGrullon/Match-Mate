// components/SportsTag.jsx
import React from "react";

const SportsTag = ({ tag }) => {
  return (
    <button className="bg-gray-200 p-2 rounded-md">{tag}</button>
  );
};

export default SportsTag;
